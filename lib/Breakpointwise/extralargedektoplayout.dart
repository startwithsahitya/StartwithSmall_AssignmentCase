import 'package:flutter/material.dart';

class ExtraLargeDesktopLayout extends StatelessWidget {
  const ExtraLargeDesktopLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Extra Large Desktop Layout (2160 and above)'),
    );
  }
}
